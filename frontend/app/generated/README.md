Generated TypeScript types from `contracts/openapi/admin-v1.json`.

`admin-api.ts` is gitignored and regenerated by `npm run generate:api` (also a
pre-hook for lint, typecheck, test, and build). Docker copies the contract into
the image and sets `ADMIN_OPENAPI_CONTRACT` before `npm run build`.
